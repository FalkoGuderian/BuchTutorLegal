dist-feed/
node_modules/
key.secret
content-index.json
course-index.json

# Klartext-Lesson-Asset-Bundles (IP) — niemals committen.
# Nur die .enc-Varianten dürfen in content/ landen.
content/**/*.json
content/*.json
