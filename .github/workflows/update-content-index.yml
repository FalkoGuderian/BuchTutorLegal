name: Update Content Index

on:
  schedule:
    - cron: '0 4 * * 1'   # Montags 4 Uhr UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Build content index
        run: python scripts/fetch_content_index.py

      - name: Commit and push
        run: |
          git config user.name "docworm-bot"
          git config user.email "bot@docworm.app"
          git add content-index.json
          if git diff --staged --quiet; then
            echo "Keine Aenderungen — kein Commit"
            exit 0
          fi
          git commit -m "chore: update content index"
          for i in 1 2 3 4 5; do
            if git push; then
              echo "Push erfolgreich (Versuch $i)"
              exit 0
            fi
            echo "Push fehlgeschlagen (Versuch $i) — rebase"
            git pull --rebase origin main || true
            sleep $((i * 2))
          done
          echo "Push nach 5 Versuchen fehlgeschlagen"
          exit 1
